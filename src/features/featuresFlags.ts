export type FeatureFlags = {
  i18n: boolean;
  cms: boolean;
  blog: boolean;
  darkMode: boolean;
};

export const features: FeatureFlags = {
  i18n: true,
  cms: true,
  blog: true,
  darkMode: true,
};
