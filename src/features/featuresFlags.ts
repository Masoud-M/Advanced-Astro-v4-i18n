export type FeatureFlags = {
  i18n: boolean;
  cms: boolean;
  demo: boolean;
  darkMode: boolean;
};

export const features: FeatureFlags = {
  i18n: true,
  cms: true,
  demo: true,
  darkMode: true,
};
